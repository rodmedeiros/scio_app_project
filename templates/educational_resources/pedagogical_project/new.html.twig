{% extends 'base.html.twig' %}


{% block form %}
    {% block content %}
        {% form_theme form 'bootstrap_3_layout.html.twig' %}
        <div class="col-md-10">
        {{ form_start(form) }}
            <div class="h3">
                <h3 class="text-dark">Novo Projeto Pedagógico</h3>
            </div><br/>
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-info mt-2">
                Salvar
            </button>
            <a href="{{ path('films') }}" class="btn btn-outline-dark mt-2">
                Voltar
            </a>
        {{ form_end(form) }}
        </div>
    {% endblock %}
{% endblock %}

